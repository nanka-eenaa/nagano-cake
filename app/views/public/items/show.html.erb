<div class="container">
  <div class="row">
    <div class="col-lg-2">
      <%= render 'public/homes/genre', genres: @genres %>
    </div>
    <div class="col-lg-10">
      <%= image_tag @item.get_image(100,100) %>
      <%= @item.name %>
      <%= @item.introduction %>
      ￥<%= @item.tax_price %>(税込)


    </div>
  </div>
</div>