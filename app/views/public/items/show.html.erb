<div class="container">
  <div class="row">
    <div class="col-lg-2">
    </div>
    <div class="col-lg-10">
      <%= image_tag @item.get_image(100,100) %>
      <%= @item.name %>
      <%= @item.introduction %>
      ￥<%= @item.price %>(税込)
      税込に変える


    </div>
  </div>
</div>