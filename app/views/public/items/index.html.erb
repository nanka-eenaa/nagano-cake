<div class="container">
  <div class="row">
    <div class="col-lg-2">
      <%= render 'public/homes/genre', genres: @genres %>
    </div>
    <div class="col-lg-10">
      <h4><strong>商品一覧</strong></h4><h6>(全<%= @all_item.count %>件)</h6>
      <% @items.each do |item| %>
        <div>
          <%= link_to item_path(item.id) do %>
            <%= image_tag item.get_image(100,100) %>
          <% end %>
          <%= item.name %>
          ￥<%= item.price %>
        </div>
      <% end %>
      <%= paginate @items %>
    </div>
  </div>
</div>