<div class="container">
  <h4>ジャンル編集</h4>
  <%= form_with model: @genre, url: admin_genre_path(@genre.id) do |f| %>
    <%= render 'form', f: f %>

    <%= f.submit "変更を保存", class: "btn btn-success" %>
  <% end %>
</div>